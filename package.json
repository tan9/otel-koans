{
  "name": "otel-koans",
  "private": true,
  "scripts": {
    "test": "jest --verbose",
    "test:smoke": "npx playwright test",
    "test:all": "jest --verbose && npx playwright test"
  },
  "jest": {
    "testPathIgnorePatterns": ["/node_modules/", "/tests/smoke/"]
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "serve": "^14.2.6"
  }
}
